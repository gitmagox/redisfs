
  The biggest omission is the handling of symlinks.

  Also missing is the handling of "utimens" - which is annoying and
 gives alerts when using either "touch" or "rsync".  But is otherwise
 harmless.

Steve
--
